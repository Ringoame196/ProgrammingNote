## 作成
```kotlin
tasks.register<Copy>("buildServer") {
    from(buildDir.resolve("libs/${project.name}-${project.version}.jar")) // コピー元
    into("Z:/discord/nushinekoBOT") // コピー先フォルダ
    rename { "bot.jar" } // ファイル名を変更
}

```
