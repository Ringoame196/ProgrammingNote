## Permission
```kotlin
permissions {
        register("trollControl.exclusion") {
            description = "チェックの除外権限"
            default = BukkitPluginDescription.Permission.Default.OP
        }
    }
```
